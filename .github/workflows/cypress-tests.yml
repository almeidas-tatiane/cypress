name: Cypress Tests

on: 
  workflow_dispatch: 


jobs: 
  cypress-electron-tests:
    runs-on: ubuntu-latest
    container: 
      image: cypress/included:cypress-15.10.0-node-24.14.0-chrome-145.0.7632.116-1-ff-148.0-edge-145.0.3800.70-1
      options: --user 1001
    steps:
      - name: Get Code - Checkout
        uses: actions/checkout@v6.0.2

      - name: Install dependencies
        run: yarn install

      - name: Run E2E tests
        run: node runner_electron.js
    env: 
      TARGET_TOKEN_ELECTRON: ${{  secrets.TARGET_TOKEN_ELECTRON  }}

  cypress-chrome-tests:
    needs: cypress-electron-tests
    runs-on: ubuntu-latest
    container: 
      image: cypress/included:cypress-15.10.0-node-24.14.0-chrome-145.0.7632.116-1-ff-148.0-edge-145.0.3800.70-1
      options: --user 1001
    steps:
      - name: Get Code - Checkout
        uses: actions/checkout@v6.0.2

      - name: Install dependencies
        run: yarn install

      - name: Run E2E tests
        run: node runner_chrome.js
    env: 
      TARGET_TOKEN_CHROME: ${{  secrets.TARGET_TOKEN_CHROME  }}

  cypress-firefox-tests:
    needs: cypress-electron-tests
    runs-on: ubuntu-latest
    container: 
      image: cypress/included:cypress-15.10.0-node-24.14.0-chrome-145.0.7632.116-1-ff-148.0-edge-145.0.3800.70-1
    steps:
      - name: Get Code - Checkout
        uses: actions/checkout@v6.0.2

      - name: Install dependencies
        run: yarn install

      - name: Run E2E tests
        run: node runner_firefox.js
    env: 
      TARGET_TOKEN_FIREFOX: ${{  secrets.TARGET_TOKEN_FIREFOX  }}